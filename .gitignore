# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env / secrets  (NEVER commit real keys)
.env
.env*.local
.env.local

# curriculum source (school's own material; ingested into Supabase, not the repo)
/curriculum/

# misc
.DS_Store
*.pem
npm-debug.log*
dev.log
.commitmsg.tmp
# scratch screenshots at repo root (not public/ reference images)
/*.png
.vercel
*.tsbuildinfo
next-env.d.ts

.vercel
